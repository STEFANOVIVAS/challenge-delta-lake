```sh
# verify python
python3 --version

# install & create env
# project [location]
pip install virtualenv
/Users/luanmorenomaciel/GitHub/ws-delta-lake-deep-dive

virtualenv venv

# activate
source venv/bin/activate

# pip install
pip install -r requirements.txt

# verify version {3.3.2}
pyspark --version
```
